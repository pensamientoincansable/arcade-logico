<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arcade Lógico - 15 Minijuegos Completos</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --dark-bg: #0a0a1a;
            --card-bg: rgba(16, 16, 32, 0.85);
            --text-primary: #ffffff;
            --text-secondary: #ccccff;
            --neon-blue: #0ff0fc;
            --neon-pink: #ff00ff;
            --neon-green: #00ff00;
            --grid-color: rgba(0, 255, 255, 0.1);
            --shadow-color: rgba(15, 240, 252, 0.5);
            --transition-speed: 0.3s;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--dark-bg);
            color: var(--text-primary);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Fondo con cuadrícula de neón */
        #gridBackground {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(var(--grid-color) 1px, transparent 1px),
                linear-gradient(90deg, var(--grid-color) 1px, transparent 1px);
            background-size: 50px 50px;
            z-index: -2;
            opacity: 0.5;
        }

        /* Puntos de neón animados */
        #neonDots {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .neon-dot {
            position: absolute;
            border-radius: 50%;
            opacity: 0.7;
        }

        @keyframes pulse {
            0% { transform: scale(1); opacity: 0.3; }
            100% { transform: scale(1.5); opacity: 0.8; }
        }

        /* Contenedor principal */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        /* Header */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            margin-bottom: 30px;
            border-bottom: 2px solid var(--neon-blue);
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo-icon {
            font-size: 2.5rem;
            color: var(--neon-blue);
            filter: drop-shadow(0 0 10px var(--neon-blue));
        }

        .logo-text h1 {
            font-size: 2rem;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-pink));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: 1px;
        }

        .logo-text p {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        .controls {
            display: flex;
            gap: 10px;
        }

        .control-btn {
            background: var(--card-bg);
            border: 1px solid var(--neon-blue);
            color: var(--text-primary);
            padding: 10px 15px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            transition: all var(--transition-speed);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .control-btn:hover {
            background: var(--neon-blue);
            color: var(--dark-bg);
            box-shadow: 0 0 15px var(--shadow-color);
        }

        /* Grid de juegos */
        .games-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .game-card {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 20px;
            transition: all var(--transition-speed);
            border: 1px solid transparent;
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .game-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-pink));
            opacity: 0;
            transition: opacity var(--transition-speed);
        }

        .game-card:hover {
            transform: translateY(-10px);
            border-color: var(--neon-blue);
            box-shadow: 0 10px 25px rgba(15, 240, 252, 0.2);
        }

        .game-card:hover::before {
            opacity: 1;
        }

        .game-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
        }

        .game-number {
            background: linear-gradient(135deg, var(--neon-blue), var(--neon-pink));
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .game-title {
            font-size: 1.3rem;
            margin-bottom: 5px;
        }

        .game-desc {
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        .game-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .game-difficulty {
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
            text-transform: uppercase;
        }

        .game-difficulty.easy {
            background: rgba(0, 255, 0, 0.1);
            color: var(--neon-green);
            border: 1px solid var(--neon-green);
        }

        .game-difficulty.medium {
            background: rgba(255, 255, 0, 0.1);
            color: #ffff00;
            border: 1px solid #ffff00;
        }

        .game-difficulty.hard {
            background: rgba(255, 0, 0, 0.1);
            color: #ff5555;
            border: 1px solid #ff5555;
        }

        .play-btn {
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-pink));
            border: none;
            color: var(--dark-bg);
            padding: 8px 20px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all var(--transition-speed);
        }

        .play-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--shadow-color);
        }

        /* Página del juego */
        #gamePage {
            display: none;
            flex-direction: column;
            min-height: 100vh;
        }

        .game-header-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background: rgba(16, 16, 32, 0.9);
            border-bottom: 2px solid var(--neon-blue);
        }

        .game-header-info h2 {
            font-size: 1.8rem;
            color: var(--neon-blue);
        }

        #gameContent {
            flex: 1;
            padding: 30px;
        }

        /* Estilos para los juegos */
        .game-container {
            max-width: 800px;
            margin: 0 auto;
            background: rgba(16, 16, 32, 0.7);
            padding: 30px;
            border-radius: 15px;
            border: 1px solid var(--neon-blue);
        }

        .game-title-large {
            font-size: 2.5rem;
            text-align: center;
            margin-bottom: 20px;
            color: var(--neon-blue);
            text-shadow: 0 0 10px var(--neon-blue);
        }

        .game-instructions {
            text-align: center;
            color: var(--text-secondary);
            margin-bottom: 30px;
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .game-controls {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 20px;
        }

        .game-input {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid var(--neon-blue);
            border-radius: 8px;
            padding: 12px 15px;
            color: var(--text-primary);
            font-size: 1rem;
            flex: 1;
            max-width: 300px;
        }

        .game-button {
            background: linear-gradient(90deg, var(--neon-blue), var(--neon-pink));
            border: none;
            color: var(--dark-bg);
            padding: 12px 25px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: bold;
            font-size: 1rem;
            transition: all var(--transition-speed);
        }

        .game-button:hover {
            transform: scale(1.05);
            box-shadow: 0 0 15px var(--shadow-color);
        }

        .game-feedback {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            border-left: 5px solid var(--neon-blue);
        }

        .game-stats {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 30px;
        }

        .stat-box {
            background: rgba(16, 16, 32, 0.9);
            padding: 20px;
            border-radius: 10px;
            border: 1px solid var(--neon-green);
            text-align: center;
            min-width: 150px;
        }

        .stat-box h3 {
            color: var(--neon-green);
            margin-bottom: 10px;
            font-size: 1rem;
        }

        .stat-box p {
            font-size: 2rem;
            font-weight: bold;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .games-grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }

            header {
                flex-direction: column;
                gap: 20px;
            }

            .controls {
                width: 100%;
                justify-content: center;
            }

            .game-controls {
                flex-direction: column;
                align-items: center;
            }

            .game-input {
                max-width: 100%;
            }

            .game-title-large {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <!-- Fondo con efectos visuales -->
    <div id="gridBackground"></div>
    <div id="neonDots"></div>
    
    <!-- Página Principal -->
    <div id="mainPage" class="container">
        <header>
            <div class="logo">
                <div class="logo-icon">
                    <i class="fas fa-gamepad"></i>
                </div>
                <div class="logo-text">
                    <h1>ARCADE LÓGICO</h1>
                    <p>15 minijuegos para entrenar tu mente</p>
                </div>
            </div>
            <div class="controls">
                <button class="control-btn" id="themeToggle">
                    <i class="fas fa-moon"></i> Tema
                </button>
            </div>
        </header>
        
        <main>
            <div class="games-grid" id="gamesGrid">
                <!-- Los juegos se cargan dinámicamente aquí -->
            </div>
        </main>
        
        <footer style="text-align: center; padding: 20px; color: var(--text-secondary);">
            <p>© 2024 Arcade Lógico - Desarrollado con <i class="fas fa-heart" style="color:#ff00ff;"></i> por Pensamiento Incansable</p>
        </footer>
    </div>
    
    <!-- Página del Juego -->
    <div id="gamePage">
        <div class="game-header-bar">
            <div class="game-header-info">
                <h2 id="currentGameTitle">Juego</h2>
            </div>
            <div class="controls">
                <button class="control-btn" id="backBtn">
                    <i class="fas fa-arrow-left"></i> Volver
                </button>
            </div>
        </div>
        
        <div id="gameContent">
            <!-- El contenido del juego se carga aquí dinámicamente -->
        </div>
    </div>
    
    <script>
        // [TODO EL JAVASCRIPT QUE ME DISTE ANTERIORMENTE SE MANTIENE IGUAL]
        // Tu código JavaScript completo va aquí...
    </script>
</body>
</html>
